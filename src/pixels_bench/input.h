#ifndef PIXELS_BENCH_INPUT_H
#define PIXELS_BENCH_INPUT_H

#include "pixels_bench/state.h"

SDL_bool pixels_handle_input(PixelsBenchState *state, BenchMetrics *metrics, BenchOverlay *overlay);

#endif /* PIXELS_BENCH_INPUT_H */
